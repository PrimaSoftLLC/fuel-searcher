spring:
  datasource:
    url: jdbc:tc:postgresql:11.1:///integration-tests-db
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        generate_statistics: true
  sql:
    init:
      mode: always

fuel-document:
  path: ./src/test/resources/postanovlenie128.2022.docx

fuel-searcher-config:
  path : ./src/test/resources/fuel-searchers.xml
